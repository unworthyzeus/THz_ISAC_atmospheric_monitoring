# Validation and Repository State

## What Was Done

The final cleanup pass focused on making the current research state reproducible and easy to continue:

1. Updated the README to describe the external data driven pipeline as the current main result.
2. Added unit tests for the external data driven forward model and template projection path.
3. Recompiled and visually checked the IEEE paper draft.
4. Tightened the LaTeX equation and result table so the IEEE two column layout fits cleanly.
5. Added Git ignore rules for raw downloaded data and generated LaTeX intermediates.
6. Rechecked that repo facing text is in English.

## Why It Was Done

The project had moved beyond the original toy sanity checks. The repository needed to clearly separate:

1. Engineering sanity checks, which may use invented toy data.
2. Main research results, which use external pollution labels and HITRAN physical line parameters.
3. Remaining simulation limitations, especially that CSI is generated by a forward model rather than measured in a field campaign.

This separation is important for academic honesty and for making the IEEE draft defensible.

## Validation Commands

The full test suite was run:

```powershell
python -m pytest tests
```

Result:

```text
7 passed
```

The English language scan was run over repo facing files:

```powershell
rg -n "<non English term scan pattern>" README.md AGENTS.md docs scripts src references paper tests
```

Result:

```text
No matches
```

## Paper Build

The IEEE paper was compiled with bundled Tectonic:

```powershell
python C:\Users\guill\.codex\plugins\cache\openai-bundled\latex\0.2.4\scripts\compile_latex.py C:\Research\THz_ISAC_atmospheric_monitoring\paper\main.tex --compiler tectonic --output-directory C:\Research\THz_ISAC_atmospheric_monitoring\paper\build --json
```

Output:

```text
paper/build/main.pdf
```

The PDF was visually inspected after rendering both pages to PNG. The result table, equations, figures, captions, and references are readable.

## Current Best Result

The best focused Ridge sweep result is:

| Feature set | Alpha | Mean normalized RMSE | Mean R2 | Max normalized RMSE |
| --- | ---: | ---: | ---: | ---: |
| `template_projection` | 17.78 | 0.07793 | 0.94523 | 0.09460 |

The broad benchmark result is effectively identical for `template_projection / ridge_10`, which suggests the optimum is not fragile.

## Data Handling

Raw downloads under `data/raw/` are ignored by Git. Processed data under `data/processed/` is also ignored because it can be regenerated from scripts. Results, figures, paper source, and paper PDF are kept in the repository.

## What Remains

1. Replace normalized HITRAN templates with calibrated HAPI absorption coefficients.
2. Improve PM scattering beyond smooth Rayleigh type trends.
3. Add atmospheric layer integration over pressure, temperature, and humidity.
4. Run sensitivity sweeps for SNR, bandwidth, elevation, and subcarrier count.
5. Add estimation bounds after the forward model becomes physically calibrated.
